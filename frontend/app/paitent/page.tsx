"use client";
import { Box, Typography } from '@mui/material';

export default function PatientsPage() {
  return (
    <Box>
      <Typography variant="h4">Patients</Typography>
      <Typography variant="body1" mt={2}>
        This is the Patients section.
      </Typography>
    </Box>
  );
}